# Forma Zieleni --- Cursor handoff 2026-09-21

This package updates the supplied Cursor OS/Canon snapshot with
Owner-in-loop decision authority and the accepted
agentic/failure-engineering principles derived from the two supplied
books.

Important: this package is a handoff/overlay, not proof of the current
local Git working tree. Cursor must reconcile it against the actual
`D:\Forma-zieleni` repo, preserve newer local work, and never blindly
overwrite files that have advanced since this snapshot.

Key current constraints: main-only; legacy CT8 untouched; no
production/DNS/secrets/real payments/customer-data actions without
explicit Owner approval; native unattended Codex write executor remains
blocked by Windows sandbox isolation; product development must continue
through safe Cursor/Owner workflow.

Use `MASTER-EXECUTION-PROMPT-V2.md` in Cursor Plan Mode, then execute
the accepted plan. The prompt is intentionally designed to continue into
real implementation (WWW/API/Portal/Admin/Mobile and later product
surfaces) rather than stop at documentation.

## Security-tool integration update --- 2026-09-21

Binding security-tool details live in
`docs/security/SECURITY-ASSURANCE.md` and the Owner onboarding flow in
`docs/security/OWNER-SETUP-CODERABBIT-OWASP.md`.

CodeRabbit is an independent review layer, not Canon authority. Preserve
`main-only`; use Cursor/IDE/CLI pre-push review rather than creating PRs
solely for CodeRabbit. Respect the verified account rate limit; until
verified, budget at most 3 free CLI reviews per developer per rolling
hour. Usage-based billing is OWNER-DECISION.

OWASP baseline: ASVS 5.0.0 requirements, ZAP DAST for runnable
local/lab/staging targets, and Dependency-Check/SCA where technically
suitable. Never active-scan legacy CT8 production or live customer
environments without explicit Owner approval.

## Security tooling

Use `docs/security/SECURITY-ASSURANCE.md`,
`docs/security/OWNER-SETUP-CODERABBIT-OWASP.md`, and
`CURSOR-PLAN-CORRECTION-CODERABBIT-OWASP.md`. CodeRabbit is
rate-limit-aware independent review; OWASP ASVS/ZAP/SCA form the free
security baseline.
