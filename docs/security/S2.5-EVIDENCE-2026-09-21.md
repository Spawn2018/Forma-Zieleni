# S2.5 evidence — 2026-09-21

## CodeRabbit

| Checkpoint | Result |
|---|---|
| Repository still public and review-safe | PASS — `gh repo view Spawn2018/Forma-Zieleni` returned `visibility: PUBLIC`, `isPrivate: false`. Diff contains no secrets or customer data. |
| Owner signs in to CodeRabbit with GitHub | PASS — Owner completed GitHub sign-in. No credentials were requested or stored. |
| Authorize only this repository | Owner reported CodeRabbit authorized for Forma Zieleni only. GitHub `user/installations` API was not readable with the current `gh` token (403); visibility was not changed. |
| GitHub App installation verified | PARTIAL — Owner confirmation accepted. API listing of installations remains 403 with the current `gh` token. |
| Official Cursor plugin | PASS — `.cursor/settings.json` has `plugins.coderabbit.enabled: true`. Official plugin cache present: `@coderabbitai/cursor-plugin` at `.cursor/plugins/cache/cursor-public/coderabbit/0066deabd63b24de1f32a249e1eb6ac77ce5585e`. No additional CodeRabbit skill was installed. |
| Official CLI | PASS — native Windows install `coderabbit`/`cr` 0.7.8 at `%LOCALAPPDATA%\Programs\coderabbit`. |
| Browser login / CLI auth | PASS — `coderabbit auth status` and `coderabbit doctor` 2026-09-21T11:43:32Z: signed in as account `Spawn2018`, provider GitHub, region US, organization `Spawn2018`, default review access `Spawn2018/Spawn2018`, plan Advanced (trial), seat assigned. Doctor: 9 passed, 0 failed, including authentication and WebSocket reachability. |
| Harmless CLI review | PASS — one completed review of committed `2e2155fc52f2463d35848c316af86dc716f6df8f` vs `origin/main`. Command: `coderabbit review --committed --base origin/main --agent`. No `--use-credits`. Started `2026-09-21T11:43:51.785Z`, completed `2026-09-21T11:49:48.559Z`, exit 0, 6 stored findings. An earlier attempt at `2026-09-21T11:38:58.837Z` failed with recoverable `TRPCWebSocketClosedError` after ~257s, produced no findings, and did not increment usage. |
| Structured `--agent` output | PASS — JSON events included `review_context`, `status`, `finding` (6), and `complete` with `outcome: completed`. |
| Live account rate limit | RECORDED, hourly cap not exhausted. Account plan is Advanced (trial). Official published Advanced trial CLI allowance is 10 reviews per developer per rolling hour. `coderabbit usage` before the completed review: Your reviews 0; after: Your reviews 1; period resets 2026-10-21. The failed WebSocket attempt did not increment the period counter. The live hourly cap was not empirically verified because that would consume further reviews. Conservative project budget remains 3 CLI reviews per developer per rolling hour until the hourly cap is measured. |
| Usage-based billing / add-ons | OFF — `coderabbit usage` reported `Usage billing : inactive` both before and after the completed review. Enabling any paid plan, overage, Security Scan add-on or Agent billing remains OWNER-DECISION. |

CodeRabbit review of committed checkpoint `2e2155fc52f2463d35848c316af86dc716f6df8f`: PASS as an independent review event. Findings were triaged and fixed in this local REVIEW slice. Remediations are not a second CodeRabbit review; the remaining CLI budget is reserved for the next materially larger coherent slice.

## Finding triage — review 2026-09-21T11:43:51Z

| Severity | Location | Disposition |
|---|---|---|
| major | `contracts/openapi.json` qualifyLead missing 409 | Valid. `qualifyLead` requires `Idempotency-Key` but omitted the shared Conflict response. Added `409` → `#/components/responses/Conflict`. |
| major | `contracts/openapi.json` public `LeadCapture.source` | Valid. Unauthenticated `POST /leads` accepted `portal` and `admin`. Public request now uses `PublicLeadSource` (`www`, `other`). Stored `Lead.source` still uses full `LeadSource`. |
| major | `packages/validation/src/lead.ts` `String(...)` coercion | Valid. Non-string JSON values were coerced. Required/optional string fields are type-checked before `normalizeCapture`. Public sources are validated against `PUBLIC_LEAD_SOURCES`. |
| major | `scripts/security/coderabbit-quota.mjs` cwd-relative log | Valid. Log now resolves from the repository root `tmp/coderabbit-reviews.json`. |
| major | `scripts/autonomous-session/native-process.test.mjs` hardcoded user path | Valid. PowerShell is resolved from `NATIVE_TEST_POWERSHELL` or `PATH`; native tests skip with a reason when `pwsh.exe` is unavailable. |
| major | `scripts/autonomous-session/engine.ts` dropped waiting slices | Valid. A missing Owner reply now leaves the slice in `pendingWork` with `DECISION_UNAVAILABLE_OR_REJECTED`. A later `resume --decision` can clear that blocker. Invalid replies still permanently block. |

No finding was skipped.

## Local REVIEW-slice checks — remediations commit

Deterministic `pnpm test`, `pnpm typecheck` and `pnpm lint` were rerun before this commit. `git diff --check` reported no conflict or whitespace errors (CRLF warnings only). `tmp/` including `tmp/coderabbit-reviews.json` stays gitignored and unstaged. Stale `SHA256SUMS.txt` is unstaged. No second CodeRabbit review was run.

Eight native Windows process tests skipped with reason `pwsh.exe not on PATH or NATIVE_TEST_POWERSHELL`. Guards were not weakened and the known native Codex executor blocker was not revisited:

- real Windows supervisor preserves argv, stdin and spaced cwd without shell expansion
- real Windows child receives only supplied environment, omitting inherited poison
- changed executable identity rejects before launch
- real Windows output flooding is bounded
- real Windows streamed Unicode survives chunk boundaries
- real Windows TIMEOUT closes job and terminates descendant
- real Windows CANCELLED closes job and terminates descendant
- already cancelled request never starts supervisor

Those skips are DEFERRED coverage for the native supervisor, not PASS.

## OWASP

| Tool | Result |
|---|---|
| ASVS 5.0.0 baseline pinned | PASS for documentation — [ASVS-5.0.0-LEAD-SLICE.md](./ASVS-5.0.0-LEAD-SLICE.md) |
| ZAP | NA — no runnable local/lab target. Official Windows packages require current Java; Docker is optional and not a prerequisite. First scan will be baseline/passive on localhost after FZ-A2. Never scan CT8. |
| Dependency-Check / SCA | NA — application runtime stack not selected. GitHub dependency alerts remain complementary once dependencies exist. |
| OWASP account | Not required. |

## Push

Push waits for an explicit Owner request. Missing ZAP/SCA remains NA, never PASS. Do not run another CodeRabbit review until the next materially larger coherent slice.
