# S2.5 evidence — 2026-09-21

## CodeRabbit

| Checkpoint | Result |
|---|---|
| Repository still public and review-safe | PASS — `gh repo view Spawn2018/Forma-Zieleni` returned `visibility: PUBLIC`, `isPrivate: false`. Diff contains no secrets or customer data. |
| Owner signs in to CodeRabbit with GitHub | DEFERRED — Owner action. Open https://www.coderabbit.ai and sign in with the GitHub account that controls Forma Zieleni. Do not paste passwords or tokens in chat. |
| Authorize only this repository | DEFERRED — Owner action. Where scoping exists, install/authorize CodeRabbit only for `Spawn2018/Forma-Zieleni`. |
| GitHub App installation verified | DEFERRED — Cursor will re-check after Owner confirms. Visibility will not be changed. |
| Official Cursor/VS Code extension | DEFERRED — Owner installs or confirms the official CodeRabbit extension, then completes extension authentication. |
| Official CLI | DEFERRED — `cr` and `coderabbit` were not on PATH. Official Windows install from [CodeRabbit CLI docs](https://docs.coderabbit.ai/cli): `irm https://cli.coderabbit.ai/install.ps1 \| iex` in PowerShell. Then open a new shell. Cursor will not run the installer unattended. |
| Browser login | DEFERRED — after CLI install, Owner runs `cr auth login` or `cr auth login --region eu`. Do not send API keys in chat. Usage-based billing stays off. |
| Harmless CLI review | DEFERRED — consumes quota. Not run. |
| Structured `--agent` output | DEFERRED — official CLI supports `cr --agent`. Capability unverified on this machine. |
| Live account rate limit | DEFERRED — published Free CLI limit is 3 reviews per developer per rolling hour ([plans](https://docs.coderabbit.ai/management/plans)). Project budget stays at that conservative number until the signed-in account shows a different live limit. |
| Usage-based billing / add-ons | OFF by project policy. Enabling any paid plan, overage, Security Scan add-on or Agent billing is OWNER-DECISION. |

CodeRabbit review of this checkpoint: DEFERRED. Not PASS.

## OWASP

| Tool | Result |
|---|---|
| ASVS 5.0.0 baseline pinned | PASS for documentation — [ASVS-5.0.0-LEAD-SLICE.md](./ASVS-5.0.0-LEAD-SLICE.md) |
| ZAP | NA — no runnable local/lab target. Official Windows packages require current Java; Docker is optional and not a prerequisite. First scan will be baseline/passive on localhost after FZ-A2. Never scan CT8. |
| Dependency-Check / SCA | NA — application runtime stack not selected. GitHub dependency alerts remain complementary once dependencies exist. |
| OWASP account | Not required. |

## Push

Push of this work waits for a verified CodeRabbit review or an explicit later Owner exception. Missing review is DEFERRED, never PASS.
