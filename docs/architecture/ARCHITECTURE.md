# Architecture — historical narrative (not binding)

Status: **SUPERSEDED as binding architecture entrypoint**.

## Binding current architecture

Use only:

**[`CURRENT-ARCHITECTURE.md`](./CURRENT-ARCHITECTURE.md)**

That file is the sole **CURRENT / BINDING** architecture source of truth.

## Provenance

This file held the Polish narrative architecture draft from the monorepo foundation pass (2026-09-20). Unique current content (modular monolith detail, outbox steps, DATA→RULES→DOMAIN→AI table, app/package map, CT8 private-origin note) has been **merged** into `CURRENT-ARCHITECTURE.md`.

Keep this file as a redirect / provenance pointer only. Do **not** maintain parallel architecture truth here. Do **not** link to this file as “current architecture”.
